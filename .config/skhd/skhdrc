#
# Filename: skhdrc
# Author:   David Oniani
# Modified: June 13, 2020
#
#      _    _         _
#  ___| | _| |__   __| |_ __ ___
# / __| |/ / '_ \ / _` | '__/ __|
# \__ \   <| | | | (_| | | | (__
# |___/_|\_\_| |_|\__,_|_|  \___|
#

# GUI applications
cmd - return : kitty --directory "$HOME/work-in-progress" --single-instance &> /dev/null
cmd - s : open -a signal
cmd - w : open -a firefox
cmd - m : open -a messages

# Terminal applications
cmd + shift - h : kitty --single-instance &> /dev/null htop
cmd + shift - n : kitty --single-instance &> /dev/null ncmpcpp
cmd + shift - m : kitty --single-instance &> /dev/null neomutt

# Disable skhd for the following apps
.blacklist [
  "firefox"
]
